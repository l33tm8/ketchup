import React from "react";
import classes from "./WorkDescription.module.css";

// user: avatar, about_user
// workStats: watch count, likes, dislikes
// description: about work

const WorkDescription = (user, workStats, description) =>
{
    return (
        <div className={classes["work-description-container"]}>

        </div>
    )
}

export default WorkDescription;